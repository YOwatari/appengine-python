# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Items
  connector: csv
    # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: asin
      external_name: asin
      # Type: String Stats: 796 properties of this type in this kind.

    - property: authors
      external_name: authors
      # Type: Text Stats: 826 properties of this type in this kind.
      import_transform: db.Text

    - property: binding
      external_name: binding
      # Type: Text Stats: 796 properties of this type in this kind.
      import_transform: db.Text

    - property: creators.name
      external_name: creators.name
      # Type: String Stats: 269 properties of this type in this kind.

    - property: creators.role
      external_name: creators.role
      # Type: String Stats: 269 properties of this type in this kind.

    - property: date
      external_name: date
      # Type: Date/Time Stats: 796 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: pages
      external_name: pages
      # Type: Integer Stats: 779 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: pic_url
      external_name: pic_url
      # Type: Text Stats: 774 properties of this type in this kind.
      import_transform: db.Text

    - property: price
      external_name: price
      # Type: Integer Stats: 796 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: publicationdate
      external_name: publicationdate
      # Type: Text Stats: 796 properties of this type in this kind.
      import_transform: db.Text

    - property: publisher
      external_name: publisher
      # Type: Text Stats: 796 properties of this type in this kind.
      import_transform: db.Text

    - property: similaritems_amazon
      external_name: similaritems_amazon
      # Type: String Stats: 3639 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: Text Stats: 796 properties of this type in this kind.
      import_transform: db.Text

    - property: url
      external_name: url
      # Type: Text Stats: 796 properties of this type in this kind.
      import_transform: db.Text


- kind: Users
  connector: csv
    # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: about
      external_name: about
      # Type: Text Stats: 13 properties of this type in this kind.
      import_transform: db.Text

    - property: date
      external_name: date
      # Type: Date/Time Stats: 13 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: fb_friends
      external_name: fb_friends
      # Type: Integer Stats: 13 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: fb_url
      external_name: fb_url
      # Type: Text Stats: 4 properties of this type in this kind.
      import_transform: db.Text

    - property: friendlist
      external_name: friendlist
      # Type: Integer Stats: 78 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: handlist
      external_name: handlist
      # Type: String Stats: 611 properties of this type in this kind.

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: location
      external_name: location
      # Type: Text Stats: 4 properties of this type in this kind.
      import_transform: db.Text

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: name
      external_name: name
      # Type: Text Stats: 4 properties of this type in this kind.
      import_transform: db.Text

    - property: token
      external_name: token
      # Type: String Stats: 13 properties of this type in this kind.

    - property: uid
      external_name: uid
      # Type: Integer Stats: 13 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)